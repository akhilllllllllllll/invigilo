import React from 'react';

const Setting = () => {
  return (
    <div className="page-container" style={{ background: 'var(--raw-sienna-50)', minHeight: '100vh', padding: '2rem' }}>
      <h2 style={{ color: 'var(--raw-sienna-900)' }}>Setting</h2>
      <p style={{ color: 'var(--raw-sienna-700)' }}>This is the Setting page. Configure application settings here.</p>
    </div>
  );
};

export default Setting;
