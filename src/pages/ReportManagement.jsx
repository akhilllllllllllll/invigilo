import React from 'react';

const ReportManagement = () => {
  return (
    <div className="page-container" style={{ background: 'var(--raw-sienna-50)', minHeight: '100vh', padding: '2rem' }}>
      <h2 style={{ color: 'var(--raw-sienna-900)' }}>Report Management</h2>
      <p style={{ color: 'var(--raw-sienna-700)' }}>This is the Report Management page. View and generate reports here.</p>
    </div>
  );
};

export default ReportManagement;
