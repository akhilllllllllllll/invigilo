import React from 'react';

const UserPermissions = () => {
  return (
    <div className="page-container" style={{ background: 'var(--raw-sienna-50)', minHeight: '100vh', padding: '2rem' }}>
      <h2 style={{ color: 'var(--raw-sienna-900)' }}>User Permissions</h2>
      <p style={{ color: 'var(--raw-sienna-700)' }}>This is the User Permissions page. Manage user roles and permissions here.</p>
    </div>
  );
};

export default UserPermissions;
