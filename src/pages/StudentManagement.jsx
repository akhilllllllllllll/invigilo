import React from 'react';

const StudentManagement = () => {
  return (
    <div className="page-container" style={{ background: 'var(--raw-sienna-50)', minHeight: '100vh', padding: '2rem' }}>
      <h2 style={{ color: 'var(--raw-sienna-900)' }}>Student Management</h2>
      <p style={{ color: 'var(--raw-sienna-700)' }}>This is the Student Management page. Add, edit, or view student information here.</p>
    </div>
  );
};

export default StudentManagement;
